// Placeholder entry point. Task 19 replaces this with the rebuilt 2D game.
// It exists so Phase 0 can prove the single-file build pipeline before any
// porting happens — a later build failure is then unambiguously the port's fault.
document.getElementById('app')!.textContent = 'words2d build stub — pipeline OK'
