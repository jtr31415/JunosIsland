/**
 * Africa — roster row 7, `ship: 7`, name band `medium`. SIXTEEN OF SIXTEEN.
 *
 * >>> **CLOSED 5 AUGUST 2026.** This file held ONE record for three days — the
 * >>> crocodile, the only survivor of thirteen quadrupeds PB-036 deleted with
 * >>> the kit route — and the fifteen below were built on the assembly route in
 * >>> one run. The header it replaces argued at length that `animal-ostrich` and
 * >>> `animal-vulture` "were never built at all, deliberately" and "must not be
 * >>> improvised" because "BOTH WANT WINGS, AND THE BANK HAS NONE". **That is no
 * >>> longer true and the change is measured, not asserted:** the `wing` role
 * >>> was baked on 4 August and the bank now carries six wing shapes, and
 * >>> `animal-goose.ts` built a long NECK out of the elephant's trunk stood on
 * >>> end. Those were the two blockers and both are gone. Nothing else in that
 * >>> header was wrong; it is retired because the world moved under it.
 *
 * `roster.ts:133-138` says which sixteen this collection holds; this file says
 * what each of them IS. Nothing here builds geometry — every record is one line
 * and the reasoning for a species' SHAPE lives in its own
 * `parts/assembled/animal-<id>.ts`, beside the number it justifies. What belongs
 * here is what is true of the COLLECTION, which is the separation work below.
 *
 * ===========================================================================
 * ## SEPARATION IS THE HARD PART OF THIS COLLECTION, AND HERE IS THE MEASURE
 * ===========================================================================
 *
 * Africa collides with the FROZEN base 24, with three built collections, and
 * with itself. The live 24 can never be edited, so every one of these
 * separations is made on this side.
 *
 * **THE SHELLS, which is the first thing a child reads.** Ten hulls exist and
 * six are spent here, and the allocation is the size story of the collection:
 *
 *       box-41  the biggest (1.350 x 1.300 x 1.350)   gorilla, buffalo
 *       box-12  the widest  (1.539 x 1.250 x 1.250)   hippo, hyena, wildebeest,
 *                                                     ostrich
 *       box-21  the tallest (1.250 x 1.505 x 1.250)   zebra
 *       box-33  the monkey's own                      baboon
 *       box-36  the panda's cube                      vulture
 *       box-31  the shallowest                        warthog
 *       box-03  the plain cube    cheetah, meerkat, antelope, mongoose,
 *                                 aardvark, crocodile
 *
 * **AGAINST THE FROZEN 24, one line each:**
 *
 *   - **cheetah** against `animal-lion` and `animal-tiger` — and against
 *     `animal-lynx` and `animal-wildcat`, which are built and on the same cube.
 *     It does NOT separate on the shell: the spots (eight `plate-10` on the
 *     pack's own card shell) and the tear lines are unique in the project and
 *     are the whole of it.
 *   - **hyena** against `animal-dog` and the built `animal-wolf`. `box-25`, the
 *     koala's dish, is the largest ear shape in the bank at 0.743 across — the
 *     next is `box-06`'s 0.482 — and no canid in the project wears an ear at
 *     all. The wolf has none.
 *   - **warthog** against `animal-hog` and `animal-pig`. It wears the hog's own
 *     ear and the hog's own nose disc, because a warthog's ARE those, and takes
 *     the ELEPHANT's tusk rather than the hog's for the one feature that has to
 *     hold the two apart.
 *   - **gorilla** and **baboon** against `animal-monkey`. The baboon takes the
 *     monkey's own shell deliberately (`animal-chicken.ts`'s argument about the
 *     frozen chick); the gorilla takes the biggest shell in the pack and has NO
 *     TAIL, which is the separation from both.
 *   - **zebra** against the built `animal-horse`, `animal-pony`, `animal-donkey`
 *     and `animal-mule`. None of the four is on `box-21`; and nothing else in
 *     the project is striped.
 *   - **buffalo** and **wildebeest** against the built `animal-ox` and
 *     `animal-water-buffalo` and the frozen `animal-cow`. Both of the built
 *     bovids are on `box-12` with the ELEPHANT's tusk for a horn; the buffalo
 *     takes `box-41` and the HOG's tusk, and the wildebeest keeps the ox's own
 *     solved horn station and separates on a beard, a black switch tail and the
 *     colour instead — because a wildebeest's horns and an ox's genuinely are
 *     the same bovid sweep and inventing a difference would be a lie.
 *
 * **AGAINST EACH OTHER, and these are the two that had to be measured:**
 *
 *   - **meerkat / mongoose.** One family, two members, and the separation is a
 *     3.7x gap in TAIL THICKNESS — `wedge-18` at 0.200 against `box-23` at
 *     0.744, the widest the bank offers between two long tails — plus the
 *     panda's card as a dark eye patch on one and the standard card on the
 *     other.
 *   - **antelope / wildebeest / buffalo.** Size first (cube, wider, biggest),
 *     then the HORN, and each takes a different shape: `cone-01` stretched into
 *     a straight spike, `wedge-11` swept, `wedge-13` driven sideways.
 *
 * **WHAT REPEATS AND WHY IT IS ALLOWED.** The bank has seven tails and this
 * collection has fifteen animals. `box-18`, the only stub, is on the hippo, the
 * antelope and the buffalo; `wedge-07` is on the zebra and the wildebeest.
 * §3.1 is the standing rule — a part's identity is its placement, its count and
 * its depth, not Kenney's label — and the two `wedge-07` cases are the honest
 * kind: a zebra's and a wildebeest's tails ARE the same long dark switch.
 *
 * ===========================================================================
 * ## WHAT IS STANDING IN FOR A SHAPE THE BANK HAS NOT GOT
 * ===========================================================================
 *
 * Joe, 5 August: *"put something in for the unbuildable ones anyway so i can do
 * it manually."* Every rostered member has an entry. Four of them lean on a
 * stand-in and every one says so in its own header's FIRST line and in its
 * `flag`, which is what he reads in the workbench:
 *
 *   - **zebra** — no stripe in the bank. Eight `plate-11` flank cards cut to
 *     0.18 of their length. The stretch is the flagged part.
 *   - **cheetah** — no facial marking in the bank. Two `plate-13` face plates
 *     cut narrow and tall for the tear lines.
 *   - **buffalo** — no CURVED shape anywhere in the bank's 100, and rule 4 as
 *     amended bakes a rotation into a copy's vertices and cannot bend one. The
 *     boss is a flat `bespoke-square-01` bar and the hook is missing.
 *   - **vulture** — no hooked beak, which `docs/how-the-animals-are-made.md`
 *     §14 named and never retired. `cone-06`'s band 15 stands 0.041900 proud of
 *     its band 13 — `animal-canary.ts`'s own measurement, taken to REFUSE the
 *     shape for a hen — and that overhang is painted dark and doing the job.
 *
 * Two more absences are mechanism rather than shape and are recorded at the
 * species: the **ostrich's legs** (one leg shape at one absolute height, so a
 * bird cannot be put on stilts) and the **gorilla's arms** (the same reason).
 *
 * ## WHY THERE ARE NO `threat` RECORDS
 *
 * Roster §5 wants statuses "true, checkable", and `Threat.checkedDate` exists so
 * a status is a dated reading of the Red List rather than a memory. Six of these
 * sixteen are genuinely threatened; writing categories here from recall would
 * produce records that LOOK checked. Absent is the honest state.
 */
import { defineSpecies } from '../define'
/*
 * Evaluated for its SIDE EFFECT, not for a name: each species module under
 * `parts/assembled/` registers its own build as it defines it, and
 * `defineSpecies` picks that up by id. Without this line every record below
 * would find no assembly and would build as a bare hull.
 * `tests/island/assembly-constants.test.ts` fails loudly if it is ever dropped.
 */
import '../parts/assembled'
import type { Species } from '../types'

/**
 * IN ROSTER ORDER, which is the order `roster.ts:133-138` lists them and the
 * order the album shows them. A collection whose file order disagrees with its
 * roster order is a trap `species-garden.test.ts:149` already exists to catch,
 * so a member arriving later is INSERTED at its rostered place, never appended.
 */
export const AFRICA_SPECIES: readonly Species[] = [

  /* The only equid in the project on the fox's TALL shell, and the only striped
   * animal anywhere in it. The stripes are stretched marking cards and flagged. */
  defineSpecies('animal-zebra', 'bespoke'),

  /* The widest shell in the pack carrying the widest muzzle, and the bank's
   * smallest ear on top of it. */
  defineSpecies('animal-hippo', 'bespoke'),

  /* Deliberately on the same cube as `animal-lynx` and `animal-wildcat`: this
   * one separates on MARKINGS — eight spots and two tear lines — not silhouette. */
  defineSpecies('animal-cheetah', 'bespoke'),

  /* The panda's card worn as a dark eye patch, and the bank's thinnest tail —
   * the two numbers that hold it apart from the mongoose below. */
  defineSpecies('animal-meerkat', 'bespoke'),

  /* The hog's own ear and nose disc, with the ELEPHANT's tusk rather than the
   * hog's, because that is the one feature separating it from a frozen animal. */
  defineSpecies('animal-warthog', 'bespoke'),

  /* The biggest shell in the pack with the silver back painted by running the
   * mammal belly line BACKWARDS — and no tail at all. */
  defineSpecies('animal-gorilla', 'bespoke'),

  /* Built 2 August, before the rest of this collection existed: the elephant's
   * trunk stretched flat as a jaw and the cat's ear repeated as dorsal scutes. */
  defineSpecies('animal-crocodile', 'bespoke'),

  /* `animal-goose.ts`'s neck, shortened and stood up — 1.5x at 45 degrees where
   * that bird is forced to 1.75x at 60 — on the widest shell. */
  defineSpecies('animal-ostrich', 'bespoke'),

  /* The slight one, and the only horn in the project that is a stretched POINT
   * rather than somebody's tusk. */
  defineSpecies('animal-antelope', 'bespoke'),

  /* The fox's brush against the meerkat's whip: 0.744 across against 0.200, the
   * widest gap the bank offers between two long tails. */
  defineSpecies('animal-mongoose', 'bespoke'),

  /* The largest ear shape in the bank, on the widest shell, against two canids
   * that have no ears at all. */
  defineSpecies('animal-hyena', 'bespoke'),

  /* The frozen monkey's own shell with the elephant's trunk cut narrow as a
   * muzzle, and a tail carried up the rear chamfer. */
  defineSpecies('animal-baboon', 'bespoke'),

  /* `animal-ox.ts`'s horn station kept unchanged on purpose — a wildebeest's
   * sweep IS an ox's — so the beard and the black switch carry the separation. */
  defineSpecies('animal-wildebeest', 'bespoke'),

  /* The hog's tusk driven sideways out of the temples, and a flat bar standing
   * in for the boss the bank cannot curve. Flagged. */
  defineSpecies('animal-buffalo', 'bespoke'),

  /* The giraffe's nose cut to twice its length as a tube snout, under the
   * bunny's ears — the tallest in the bank, at 2.010 with 0.010 to spare. */
  defineSpecies('animal-aardvark', 'bespoke'),

  /* The lion's MANE worn as a neck ruff, which nothing had spent, and the
   * nearest thing the bank has to a hooked bill. Flagged. */
  defineSpecies('animal-vulture', 'bespoke'),
]
