/**
 * The Outback collection — roster row 11, `ship: 11`, name band `medium`.
 *
 * NEW FILE, 5 August 2026. There has never been a `collections/outback.ts`; this
 * one was written on the assembly route from the first line.
 *
 * ===========================================================================
 * ## WHAT THE DOCS SAID, AND WHAT MEASURING ACTUALLY FOUND
 * ===========================================================================
 *
 * `docs/how-the-animals-are-made.md` §14 has said since 29 July that
 * *"Legendary (12) and **Outback (16)** are near-total failures"* — grouped with
 * Ocean, Critters, Dinosaurs and Raptors under a list of shapes the pack does
 * not hold. **That sentence is now the second one in this repo to be measured
 * and found wrong**, and it was wrong in the same way Ocean's was: it counts a
 * part by the name Kenney gave it rather than by its shape, which is exactly
 * what §3.1 says not to do.
 *
 * The bank was re-censused against this collection's sixteen, over all 100
 * baked records rather than against the table in §7. **Thirteen of the sixteen
 * are real animals and three are placeholders**, and not one of the three is
 * held up by anything §14 names:
 *
 *   - **The platypus is NOT blocked.** Its bill is `blade-05`, the lion's flat
 *     muzzle plate, and its tail is `wedge-03` — the BEAVER'S OWN PADDLE, which
 *     is the comparison every guide to that animal makes. It is on `box-13`, the
 *     crab's plate and the only flat hull in the bank, which Joe's 3 August
 *     ruling on the height band made usable and which Ocean's ray and starfish
 *     spent first.
 *   - **The echidna and the thorny devil are NOT blocked.** Both are the
 *     hedgehog's repeat-and-sink on `cone-01`, which §8's chamfer idiom was
 *     built for and which the pufferfish and the sea urchin have since proved
 *     twice more.
 *   - **The two ratites are NOT blocked.** `animal-goose.ts` built a NECK out of
 *     the elephant's trunk stood on end and `animal-ostrich.ts` shortened it;
 *     the emu takes it shorter and leaned further again.
 *   - **The four birds are NOT blocked.** `animal-chicken.ts`'s galliform idiom
 *     and `animal-kingfisher.ts`'s carry all four between them.
 *
 * ===========================================================================
 * ## THE SHELLS, WHICH IS THE FIRST THING A CHILD READS
 * ===========================================================================
 *
 * Seven of the pack's ten hulls are spent here, which is more than any
 * collection so far, and the allocation IS the size story:
 *
 *       box-03  the plain 1.250 cube      quokka, cockatoo, dingo, bilby,
 *                                         numbat, bandicoot, lyrebird
 *       box-12  the widest  (1.5395)      wombat
 *       box-13  the crab's plate, the     platypus
 *               ONLY flat hull (0.4506)
 *       box-20  the fish's cube           emu
 *       box-31  the lion's, shallowest    echidna, frilled lizard, thorny devil
 *               (1.125 deep)
 *       box-33  the monkey's cube         kangaroo
 *       box-39  the penguin's, TWO bands  kookaburra, tassie devil
 *
 * **`box-41` IS DELIBERATELY UNSPENT AND HERE IS THE MEASUREMENT.** It is the
 * biggest shell in the bank and both the wombat and the devil reached for it.
 * Its front face stands at z = 0.725 — 0.090 IN FRONT of `EYE_CARD_Z`, the
 * absolute 0.6350 an eye card is pinned to on all 48 of the pack's own — and
 * `collections/ocean.ts` already recorded that an eye placed LOW on it is buried
 * inside the hull. Measured further here, because the trap has a second half:
 * its flat front plate is only **|x| ≤ 0.200 and y 0.4938 to 0.8938**, so the
 * default eye station (x 0.263, y 0.934) is off the plate in BOTH directions,
 * and above y = 0.894 the surface falls back to z = 0.3125 — so an eye there
 * floats in mid-air instead. Nothing in this collection wears it.
 *
 * **`box-21` IS ALSO UNSPENT, for `animal-wolf.ts`'s reason.** That shell is a
 * 1.250 cube with two fused EAR LUGS on top reaching 1.6863, which is why the
 * wolf has no ear feature. The kangaroo wanted the tallest hull and would have
 * arrived with four ears, so it is on `box-33` instead.
 *
 * ===========================================================================
 * ## ROSTER §4 — the separations, and the three that had to be measured
 * ===========================================================================
 *
 * **AGAINST THE FROZEN 24 AND THE BUILT COLLECTIONS:**
 *
 *   - **dingo** against the FROZEN `animal-dog` and `animal-fox`, and against
 *     the built `animal-wolf` and `animal-fennec-fox`. It wears the DOG's own
 *     ear and nose deliberately — a dingo is a dog — and separates from the
 *     fennec, which shares its `box-23` brush, by painting Kenney's band 5 the
 *     other way about: a WHITE tip where that animal has a black one.
 *   - **emu** against `animal-ostrich`. Priced in full in its own file: a
 *     narrower shell, a shorter and more leaned neck, and no wing and no tail.
 *   - **cockatoo** against `animal-cockatiel`, which is the same family. Five
 *     `cone-01` crest points at the shape's own burial against that bird's
 *     three, on `animal-chicken.ts`'s own measured five-point solve.
 *   - **kookaburra** against `animal-kingfisher`, deliberately sharing its
 *     shell, bill and wing: the tail is 1.8x the reach and the palette is its
 *     opposite.
 *   - **echidna** and **thorny devil** against `animal-hedgehog` and
 *     `animal-porcupine`, and against `animal-pufferfish` and
 *     `animal-sea-urchin`. All six spend `cone-01`, because it is the only shape
 *     in 100 that puts a point on a body, so all six separate on ROW SET, COUNT
 *     and SPIN — §3.1 as arithmetic. Each file states its own.
 *   - **bilby** against the FROZEN `animal-bunny` and the built `animal-hare`
 *     and `animal-fennec-fox`, all of which wear `box-06`. Three animals with
 *     genuinely outsized ears; the separation is the long `tube-07` snout, the
 *     two-tone tail and a blue-grey coat nothing else in the project has.
 *
 * **AGAINST EACH OTHER, and these are the three that were measured:**
 *
 *   - **bilby / numbat / bandicoot.** Three small long-nosed marsupials, all
 *     three on `tube-07`, which is honest — they all have one. The separations
 *     are the EAR (`box-06` at 0.913 against `wedge-04` at 0.341, a 2.7x gap
 *     with nothing usable between them) and the TAIL (a two-tone whip, the fox's
 *     brush carried UP the rear chamfer, and a bare whip trailing low).
 *   - **kangaroo / quokka.** Both macropods, and neither can stand like one. The
 *     quokka gets away with it because it is small and squats; the kangaroo does
 *     not, and is a placeholder for it. Ear again: `box-06` at 0.913 against
 *     `box-02` at 0.315, a 2.9x gap.
 *   - **echidna / thorny devil.** Both on `box-31`, both `cone-01` on all three
 *     row kinds. Separated by COUNT (three a row against four), by SPIN (the
 *     echidna's swept back over the rump, the devil's unspun and radial), by the
 *     devil's two `cone-04` brow horns, and by the echidna's 0.532 beak.
 *
 * ===========================================================================
 * ## THE THREE PLACEHOLDERS, PRICED — and the four things they wait on
 * ===========================================================================
 *
 * Joe, 5 August: *"put something in for the unbuildable ones anyway so i can do
 * it manually. if there is no entry at all, i cant do that."* So each is a full
 * entry — file, index line, record, `MOVES`, both ledger rows — carrying the
 * nearest honest approximation, with the first line of its own header saying it
 * is a placeholder and what exactly is wrong. The kangaroo waits on two things
 * and the lyrebird on two; the frilled lizard on one. What they are:
 *
 * **1. A LONG HIND LEG — the kangaroo, and it is the biggest single commission
 * this collection implies.** `box-01` is the ONLY leg shape in the bank, it
 * occurs 86 times across 23 of the 24 originals, it is 0.30625 tall, and it sits
 * at one absolute row height because that is what puts feet on y = 0.
 * `animal-ostrich.ts` hit this first and said there is no dial that lengthens a
 * leg. **One commissioned leg would improve four animals** — the kangaroo, the
 * quokka, the emu and the ostrich.
 *
 * **2. THE HULL CANNOT BE STOOD UP — the kangaroo again.** `HullDef` is `part`,
 * `at` and `paint`, and `stretch` is `never` on Joe's own ruling. There is no
 * `spin`. A kangaroo's trunk is upright and rule 3 forbids splitting it into a
 * chest and a haunch, so the posture is unsayable at any price. This is a
 * builder-side gap rather than a missing shape and it is stated as one.
 *
 * **3. A DISC — the frilled lizard.** Nothing in 100 shapes is a circle; the
 * only zero-thickness parts are the eye cards and four marking cards, the
 * biggest 0.400 x 0.433. **The authored `bespoke-circle-01` was considered and
 * REFUSED**, and that refusal is the load-bearing part: `authored.ts` quotes
 * JT-041 as *"PRIMITIVES ONLY... Not a fin, flipper, fluke, membranous wing,
 * segmented leg, **FRILL**..."* — Joe listed a frill by name as a thing the
 * three base shapes are not for. Four `box-25` koala dishes stand in, stepped
 * around the body on §8's chamfer idiom. Giving the circle is one line and it is
 * his.
 *
 * **4. A CURVE AND A FILAMENT — the lyrebird.** All 100 shapes are straight or
 * tapered along a single axis, and rule 4 as amended bakes a ROTATION into a
 * copy — it turns a part and cannot bend one. **This is the THIRD collection to
 * price the same missing curve**, after the seahorse (`collections/ocean.ts`)
 * and the flamingo's bill (`collections/birds.ts`), which makes it evidence
 * rather than coincidence. The filament is separate and worse: nothing in the
 * bank is a thin translucent sheet and the texture route has no alpha, which
 * `animal-jellyfish.ts` reached from the other end.
 *
 * ## ONE ANIMAL DECLARES RULE 9
 *
 * `animal-thorny-devil` is 1,080 triangles against the pack's measured 422–951,
 * and its `flag` names RULE 9 as `creature.ts` demands. Twenty-two protrusions
 * is 804 of that. `animal-hedgehog` carries the same declaration at 1,021 and
 * the same argument: rule 9's own budget is VERTICES and 796 is well inside
 * 405–1626. Four members are UNDER the band instead — the platypus, the emu, the
 * kookaburra and the lyrebird — which reports rather than fails since 3 August,
 * and each says so in its own flag.
 *
 * ## WHY THERE ARE NO `threat` RECORDS
 *
 * Roster §5 wants statuses "true, checkable", and `Threat.checkedDate` exists so
 * a status is a dated reading of the Red List rather than a memory. Writing
 * categories here from recall would produce records that LOOK checked. Absent
 * means "not recorded yet", which is honest — and it matters here, because
 * several of these sixteen genuinely are listed.
 */
import { defineSpecies } from '../define'
/*
 * Evaluated for its SIDE EFFECT, not for a name: each species module under
 * `parts/assembled/` registers its own build as it defines it, and
 * `defineSpecies` picks that up by id. Without this line every record below
 * would find no assembly and would build as a bare hull.
 */
import '../parts/assembled'
import type { Species } from '../types'

/**
 * ALL SIXTEEN, and the two lists are kept separate on purpose.
 *
 * THIRTEEN ARE REAL ANIMALS. THREE ARE PLACEHOLDERS — the kangaroo, the frilled
 * lizard and the lyrebird, each saying so in the first line of its own header
 * and in its `flag`. A placeholder is not promoted into the built count to make
 * the number look better; the survey above prices every one.
 *
 * IN ROSTER ORDER, which is the order `roster.ts:205-211` lists them and the
 * order the album shows them. A collection whose file order disagrees with its
 * roster order is a trap `species-garden.test.ts:149` already exists to catch,
 * so a member arriving later is INSERTED at its rostered place, never appended.
 *
 * Every record below is one line. The reasoning for a species' SHAPE lives in
 * its own `parts/assembled/animal-<id>.ts`, beside the number it justifies — and
 * for a placeholder, so does the price of finishing it, because that is where
 * the person doing it by hand will be reading.
 */
export const OUTBACK_SPECIES: readonly Species[] = [

  /* PLACEHOLDER — the bank has one leg shape at one absolute height and a hull
   * carries no rotation, so a kangaroo's whole posture is unsayable. */
  defineSpecies('animal-kangaroo', 'bespoke'),

  /* The widest shell in the pack under the pack's deepest-set ear, and the only
   * member here with no tail at all. */
  defineSpecies('animal-wombat', 'bespoke'),

  /* The animal `box-13` was waiting for: the pack's one flat hull, the beaver's
   * own paddle laid flat, and the lion's muzzle plate cut down as a bill. */
  defineSpecies('animal-platypus', 'bespoke'),

  /* `animal-ostrich.ts`'s neck at 1.25x and 55 degrees where that bird is 1.5x
   * and 45 — and separated as much by what it has NOT got. */
  defineSpecies('animal-emu', 'bespoke'),

  /* The hedgehog's five rows at three a row, under the only 0.532 straight beak
   * in the bank. */
  defineSpecies('animal-echidna', 'bespoke'),

  /* The small round-faced one, and the cheapest animal here: it is a macropod
   * that squats, so a four-legged round animal is close to the truth. */
  defineSpecies('animal-quokka', 'bespoke'),

  /* Four parrot parts on one bird, because it is a parrot — with five crest
   * points on `animal-chicken.ts`'s own measured headroom. */
  defineSpecies('animal-cockatoo', 'bespoke'),

  /* The first canid in the project to wear a real ear, and it is the DOG's. */
  defineSpecies('animal-dingo', 'bespoke'),

  /* PLACEHOLDER — no disc anywhere in the bank, and JT-041 names a FRILL as a
   * thing the three base shapes are not for. Four koala dishes stand in. */
  defineSpecies('animal-frilled-lizard', 'bespoke'),

  /* `animal-kingfisher` grown up: the same shell, bill and wing, separated on a
   * tail 1.8x the reach and the opposite palette. */
  defineSpecies('animal-kookaburra', 'bespoke'),

  /* The white chest is Kenney's own forward band on `box-39` — one entry, no
   * geometry, and the one marking a child is told about this animal. */
  defineSpecies('animal-tassie-devil', 'bespoke'),

  /* The bank's biggest ear over its longest snout, with a two-tone tail that is
   * `animal-stoat.ts`'s idiom run backwards. */
  defineSpecies('animal-bilby', 'bespoke'),

  /* The squirrel's carried tail on a striped animal — and the stripes are the
   * zebra's mechanism WITHOUT its stretch. */
  defineSpecies('animal-numbat', 'bespoke'),

  /* The plain one of the three long-nosed marsupials, on purpose: a collection
   * of sixteen needs one. */
  defineSpecies('animal-bandicoot', 'bespoke'),

  /* Twenty thorns UNSPUN so they stand radial rather than sweeping back, plus
   * two hog-ear brow horns. Declares RULE 9. */
  defineSpecies('animal-thorny-devil', 'bespoke'),

  /* PLACEHOLDER — the tail is the animal and it wants a curve and a filament,
   * neither of which the bank has. The galliform body under it is honest. */
  defineSpecies('animal-lyrebird', 'bespoke'),
]
